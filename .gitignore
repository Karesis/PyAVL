# ----------------------------------------------------------------
# .gitignore for the pyavl project
# ----------------------------------------------------------------

# 1. 虚拟环境
# 永远不要提交你的虚拟环境
.venv/

# 2. Python 缓存文件
# 这些是 Python 自动生成的字节码缓存，会降低跨平台兼容性
__pycache__/
*.pyc

# 3. 构建与打包产物
# 这些是运行 setup.py 或 pip install 时生成的临时文件夹和安装元数据
build/
dist/
*.egg-info/

# 4. 编译好的 C 扩展模块
# 这些是二进制文件，应该在需要时由源码重新编译，而不是直接存入仓库
*.pyd
*.so
*.dll

# 5. 测试与覆盖率报告
# 这些是运行测试后生成的缓存和报告，属于“结果”而不是“源码”
.pytest_cache/
.coverage
htmlcov/

# 6. 常见的 IDE/编辑器 配置文件
# 让项目在不同开发者的编辑器中保持一致，不提交个人配置
.vscode/
.idea/

# 7. 常见的操作系统生成文件
.DS_Store
Thumbs.db